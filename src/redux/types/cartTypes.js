// Defines the action type for when an item is added to the cart
export const ADD_TO_CART = "ADD_TO_CART";

// Defines the action type for when an item is removed from the cart
export const REMOVE_FROM_CART = "REMOVE_FROM_CART";

// Defines the action type for when the quantity of an item in the cart is adjusted
export const ADJUST_ITEM_QUANTITY = "ADJUST_ITEM_QUANTITY";

// Defines the action type for when the cart should be cleared entirely
export const CLEAR_CART = "CLEAR_CART";

// Defines the action type for when cart items are loaded from an external source (e.g., local storage or a backend)
export const LOAD_CART_ITEMS = "LOAD_CART_ITEMS";

// Defines the action type for when the checkout process is initiated
export const CHECKOUT_REQUEST = "CHECKOUT_REQUEST";

// Defines the action type for when the checkout process is successful
export const CHECKOUT_SUCCESS = "CHECKOUT_SUCCESS";

// Defines the action type for when the checkout process fails
export const CHECKOUT_FAILURE = "CHECKOUT_FAILURE";

export const ADJUST_QUANTITY = "ADJUST_QUANTITY";
